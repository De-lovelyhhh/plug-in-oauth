let data = require('./api/data.js')

module.exports = {
    getData: data.getData,
    setData: data.setData,
    getConfigure: data.getConfigure,
    getCode: data.getCode,
    redirect: data.redirect,
    getSkey: data.getSkey
}
